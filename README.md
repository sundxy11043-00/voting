# ระบบการเลือกตั้งออนไลน์ 2569 - Thai Election Voting System

ระบบเว็บเลือกตั้งออนไลน์ที่ใช้ Node.js, Express, SQLite สำหรับการลงคะแนนเสียงในการเลือกตั้งไทยปี 2569

![Thai Election Voting System](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-ISC-green.svg)
![Node.js](https://img.shields.io/badge/node-%3E%3D14.0.0-brightgreen.svg)

## ✨ ฟีเจอร์หลัก

- 🔐 **สมัครสมาชิก**: ใส่เลขประจำตัว (5 หลัก) และสร้างรหัสผ่าน
- 🔑 **เข้าสู่ระบบ**: ด้วยเลขประจำตัวและรหัสผ่าน
- 🗳️ **การลงคะแนน**: เลือกจากพรรคการเมืองไทย 10 พรรค
- ⏰ **ลงคะแนนครั้งเดียว**: ผู้ใช้สามารถลงคะแนนได้เพียงครั้งเดียวเท่านั้น
- 📊 **ผลการลงคะแนนแบบสด**: แสดงคะแนนแต่ละพรรคแบบเรียลไทม์ (Web Socket)
- 🔄 **ตรวจสอบสถานะ**: หากผู้ใช้เข้าสู่ระบบซ้ำ จะไปยังหน้าผลการลงคะแนนทันที
- ✅ **เลขประจำตัวไม่ซ้ำ**: ระบบตรวจสอบและแจ้งเตือนหากเลขซ้ำกัน
- 💾 **ตรวจสอบรหัสผ่าน**: รหัสผ่านเข้ารหัสด้วย bcryptjs

## 🛠️ ข้อกำหนด

- Node.js เวอร์ชัน 14.0.0 ขึ้นไป
- npm 6.0.0 ขึ้นไป
- Git (สำหรับการ clone และ push)

## 📥 การติดตั้ง

### 1. Clone Repository
```bash
git clone https://github.com/YOUR_USERNAME/thai-election-voting-system.git
cd thai-election-voting-system
```

### 2. ติดตั้ง Dependencies
```bash
npm install
```

### 3. รันเซิร์ฟเวอร์
```bash
npm start
```

เซิร์ฟเวอร์จะเริ่มทำงานที่ `http://localhost:3000`

## 🚀 วิธีใช้งาน

### สมัครสมาชิก
1. คลิก "สมัครสมาชิก" ที่หน้าแรก
2. ใส่เลขประจำตัว 5 หลัก (เช่น 12345)
3. ใส่รหัสผ่านตัวเอง (อย่างน้อย 6 ตัวอักษร)
4. คลิก "สมัครสมาชิก"

### เข้าสู่ระบบ
1. ใส่เลขประจำตัวที่สมัครไว้
2. ใส่รหัสผ่าน
3. คลิก "เข้าสู่ระบบ"

### ลงคะแนน
1. เลือกพรรคการเมืองที่ต้องการ
2. คลิก "ยืนยันการลงคะแนน"
3. จะไปยังหน้าแสดงผลการลงคะแนนแบบสด

## 📁 โครงสร้างโปรเจคต์

```
thai-election-voting-system/
├── server.js                  # Express server หลัก
├── package.json              # Dependencies และ scripts
├── .gitignore               # ไฟล์ไม่ต้องเก็บใน git
├── README.md                # ไฟล์นี้
├── voting.db               # SQLite database (สร้างอัตโนมัติ)
└── public/
    ├── index.html          # หน้า HTML หลัก
    ├── styles.css          # Stylesheet
    └── app.js              # Frontend JavaScript
```

## 🔌 API Endpoints

| Method | Endpoint | Description | Request Body |
|--------|----------|-------------|--------------|
| POST | `/api/register` | สมัครสมาชิก | `{ citizen_id, password }` |
| POST | `/api/login` | เข้าสู่ระบบ | `{ citizen_id, password }` |
| GET | `/api/voting-data` | ดึงข้อมูลการลงคะแนน | - |
| POST | `/api/vote` | ลงคะแนน | `{ party_id }` |
| GET | `/api/results` | ดึงผลการลงคะแนน | - |
| POST | `/api/logout` | ออกจากระบบ | - |

## 🎭 พรรคการเมืองไทย (2569)

1. **เพื่อไทย** (Pheu Thai)
2. **ก้าวไกล** (Move Forward)
3. **พืชศรุษ** (Bhumjaithai)
4. **ประชาธิปไตย** (Democrat)
5. **ชาติไทย** (Thai Nation)
6. **คนไทยสร้างไทย** (Thai Build Thailand)
7. **สยาม** (Siam)
8. **พลังประชารัฐ** (Popular Force)
9. **ประชาชนใจสุไทย** (Thai People)
10. **อนาคตไทย** (Future Thailand)

## 🔒 ความปลอดภัย

✅ รหัสผ่านเข้ารหัสด้วย **bcryptjs**
✅ **Session management** ด้วย express-session
✅ ตรวจสอบการลงคะแนนซ้ำอัตโนมัติ
✅ เลขประจำตัวไม่ซ้ำกันในระบบ
✅ Data validation บน server-side

## 🛠️ เทคโนโลยีที่ใช้

- **Backend**: 
  - Node.js
  - Express.js
  - SQLite3
  - bcryptjs (Password hashing)
  - express-session
  - Socket.io (Real-time updates)

- **Frontend**: 
  - HTML5
  - CSS3
  - Vanilla JavaScript
  - Socket.io Client

## 📊 ฐานข้อมูล

### ตาราง Users
```
id: INTEGER (Primary Key)
citizen_id: TEXT (Unique) - เลขประจำตัว
password: TEXT - รหัสผ่านที่เข้ารหัส
has_voted: INTEGER (0=ยังไม่ลงคะแนน, 1=ลงคะแนนแล้ว)
created_at: DATETIME - วันเวลาสมัครสมาชิก
```

### ตาราง Parties
```
id: INTEGER (Primary Key)
name: TEXT (Unique) - ชื่อพรรค
vote_count: INTEGER - จำนวนคะแนน
```

### ตาราง Votes
```
id: INTEGER (Primary Key)
user_id: INTEGER (Foreign Key)
party_id: INTEGER (Foreign Key)
voted_at: DATETIME - วันเวลาลงคะแนน
```

## 🌐 การอัพโหลด GitHub

### 1. สร้าง Repository ใน GitHub
1. ไปที่ https://github.com/new
2. กรอกชื่อ repository (เช่น `thai-election-voting-system`)
3. คลิก "Create repository"

### 2. Push ไป GitHub
```bash
# Navigate to project folder
cd thai-election-voting-system

# Initialize git และ set remote
git init
git remote add origin https://github.com/YOUR_USERNAME/thai-election-voting-system.git

# Add all files
git add .

# Commit
git commit -m "Initial commit: Thai election voting system"

# Push to GitHub
git branch -M main
git push -u origin main
```

### 3. วิธีให้คนอื่นใช้งาน
ส่วนคนอื่นสามารถติดตั้งและใช้งานได้โดย:

```bash
# Clone repository
git clone https://github.com/YOUR_USERNAME/thai-election-voting-system.git

# เข้าไปในโปรเจคต์
cd thai-election-voting-system

# ติดตั้ง dependencies
npm install

# รัน server
npm start

# เปิดเบราว์เซอร์และไปที่ http://localhost:3000
```

## 🚀 Deployment (Deploy ไป Server จริง)

### Option 1: Heroku (ฟรี)
1. สมัครที่ https://www.heroku.com
2. ติดตั้ง Heroku CLI
3. รันคำสั่ง:
```bash
heroku create your-app-name
git push heroku main
heroku open
```

### Option 2: Railway (ฟรี)
1. ไปที่ https://railway.app
2. Connect GitHub repository
3. Deploy อัตโนมัติ

### Option 3: DigitalOcean / Linode / AWS
ต้องมี VPS และ setup server เอง

## 📝 Environment Variables (ถ้าต้องการ)

สร้างไฟล์ `.env`:
```
PORT=3000
NODE_ENV=development
SESSION_SECRET=your-secret-key
```

## 🐛 การแก้ไขปัญหาทั่วไป

### Port 3000 already in use
```bash
# Windows
netstat -ano | findstr :3000
taskkill /F /PID <PID>

# Mac/Linux
lsof -i :3000
kill -9 <PID>
```

### npm install ผิดพลาด
```bash
# ลบ node_modules และ package-lock.json
rm -rf node_modules package-lock.json

# ติดตั้งใหม่
npm install
```

### Database error
```bash
# ลบ voting.db และ restart server
rm voting.db
npm start
```

## 📈 พัฒนาเพิ่มเติมในอนาคต

- [ ] Authentication ด้วย OTP ผ่าน SMS/Email
- [ ] Admin Dashboard สำหรับดูสถิติ
- [ ] Export ผลคะแนนเป็น PDF/Excel
- [ ] Dark mode
- [ ] Mobile app (React Native)
- [ ] Database backup automatic
- [ ] Two-factor authentication
- [ ] Real-time notifications

## 📧 ติดต่อและสนับสนุน

หากมีปัญหาหรือข้อเสนอแนะ สามารถ:
- สร้าง Issue ใน GitHub
- ส่ง Pull Request
- ติดต่อผ่าน Email

## 📄 ลิขสิทธิ์

ISC License - สามารถใช้งานและแก้ไขได้อย่างอิสระ

## 🙏 ขอบคุณ

ระบบนี้สร้างสำหรับการเลือกตั้งไทยปี 2569
เพื่อให้ระบบการลงคะแนนออนไลน์ชัดเจน โปร่งใส และงานง่าย

---

**Made with ❤️ for Thai Election 2569**

